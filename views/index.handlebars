<div class="container">
<h1>Burgers!</h1>

<h2>Burgers to be Eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burgers-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers half Eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burgers-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Fresh</label><br>
    <input type="radio" name="devoured" value="1" checked> Fresh!<br>
    <input type="radio" name="devoured" value="0"> Half Eaten
  </div>

  <button type="submit">Add Burger</button>
</form>
</div>